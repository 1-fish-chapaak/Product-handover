/**
 * Platform Usage.
 *
 * The UI layer was deleted and is being rebuilt from `PRD-PLATFORM-USAGE.md`.
 * The record layer, `snapshot()` and both exports survived the wipe on purpose:
 * they are what make every figure on this page the customer's own fact rather
 * than a claim, and nothing about them was wrong.
 *
 * What goes here is specified in the PRD. In short: one page, a Viewing as
 * switch over three views for four readers, leading with what the quarter's
 * audit work would have cost in human time against what it cost with us, and
 * the price of an auditor hour carrying its whole derivation on screen beside
 * every figure it produces.
 */
export default function PlatformUsageView() {
  return null;
}
